# Baseline experiment

Run from the repository root:

```bash
python tools/run_experiment.py experiments/baseline/experiment.yaml
```

The generated manifest records source hashes, environment details, command logs, return codes, durations, and collected JSON metrics.
