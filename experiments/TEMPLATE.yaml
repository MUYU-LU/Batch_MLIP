id: replace-me
description: Describe the single change being evaluated.
hypothesis: State a falsifiable prediction and mechanism.
environment: {}
commands:
  - name: tests
    command: [python, -m, pytest, -q]
  - name: make-demo
    command: [python, -m, batch_mlip, make-demo, data/demo.extxyz]
  - name: validation
    command: [python, -m, batch_mlip, validate, configs/relax_toy.yaml]
  - name: benchmark
    command: [python, benchmarks/benchmark_scaling.py, --output, runs/candidate_benchmark.json]
collect_json:
  - validation.json
  - runs/candidate_benchmark.json
